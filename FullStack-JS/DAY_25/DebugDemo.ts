//Example of debugging in TypeScript using VS Code:
function add(a: number, b: number): number {
    return a + b;
}
const result = add(5, 10);
console.log("Result:", result);
// In this example, you can set a breakpoint on the line where the add function is called or on the line where the result is logged. 
// When you start debugging, you can step through the code to see how the add function is executed and inspect the values of a, b, and result at each step.
//  This allows you to identify any issues or unexpected behavior in your code and fix them accordingly.

// Example of a breakpoint in VS Code:
function multiply(x: number, y: number): number {
    return x * y;
}
const product = multiply(4, 5);
console.log("Product:", product);
// In this example, you can set a breakpoint on the line where the multiply function is called or on the line where the product is logged. 
// When you start debugging, you can step through the code to see how the multiply function is executed and inspect the values of x, y, and product at each step. 
// This allows you to identify any issues or unexpected behavior in your code and fix them accordingly.


